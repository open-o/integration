#
# This file was auto-generated by gen-all-dockerfiles.sh; do not modify manually.
#
# ./common-services-auth/Dockerfile
#

# 10-basebuild.txt

FROM centos:7

RUN sed -i 's|#baseurl=http://mirror.centos.org/centos|baseurl=http://mirrors.ocf.berkeley.edu/centos|' /etc/yum.repos.d/*.repo
RUN yum update -y

RUN yum install -y wget unzip socat java-1.8.0-openjdk-headless
RUN sed -i 's|#networkaddress.cache.ttl=-1|networkaddress.cache.ttl=10|' /usr/lib/jvm/jre/lib/security/java.security
ENV JAVA_HOME /usr/lib/jvm/jre

WORKDIR /service

# 15-python.txt
RUN yum -y install epel-release && yum -y install python-{devel,virtualenv,setuptools,pip} openssl-devel mysql-devel gcc libffi-devel redis
# 20-mysq.txt

# Set up mysql
RUN wget -q http://repo.mysql.com/mysql-community-release-el7-5.noarch.rpm && rpm -ivh mysql-community-release-el7-5.noarch.rpm && rm -f mysql-community-release-el7-5.noarch.rpm
RUN yum -y update
RUN yum -y install -y mysql-server
RUN mysql_install_db --user=mysql --datadir=/var/lib/mysql
COPY init-mysql.sh .

# 30-tomcat.txt

# Set up tomcat
RUN wget -q http://mirrors.ocf.berkeley.edu/apache/tomcat/tomcat-8/v8.5.6/bin/apache-tomcat-8.5.6.tar.gz && tar --strip-components=1 -xf apache-tomcat-8.5.6.tar.gz && rm -f apache-tomcat-8.5.6.tar.gz
ENV CATALINA_HOME /service
# 50-microservice.txt - AUTOGENERATED, DO NOT MODIFY MANUALLY

# Set up microservice
RUN wget -q -O common-services-auth-1.0.0.zip https://nexus.open-o.org/content/repositories/autorelease-1086/org/openo/common-services/auth/auth-service-deployment/1.0.0/auth-service-deployment-1.0.0.zip && unzip -q -o -B common-services-auth-1.0.0.zip && rm -f common-services-auth-1.0.0.zip
EXPOSE 8102
RUN echo Open-O 1.0.0 autorelease-1086 > OPENO_VERSION


# 51-permissions.txt
RUN chmod +x bin/*.sh
# 52-keystone.txt
RUN yum -y update
RUN yum -y install httpd
RUN yum -y install https://repos.fedorapeople.org/repos/openstack/openstack-liberty/rdo-release-liberty-5.noarch.rpm
RUN yum -y install openstack-keystone mod_wsgi memcached python-memcached python-openstackclient
# 90-entrypoint.txt

RUN yum clean all

COPY instance-config.sh .
COPY instance-init.sh .
COPY instance-run.sh .
COPY instance-workaround.sh .
COPY docker-entrypoint.sh .
ENTRYPOINT /service/docker-entrypoint.sh

COPY LICENSE ./OPENO_LICENSE
